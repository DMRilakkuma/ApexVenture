server:
  port: 8503
  servlet:
    context-path: /oauth-server   # 服务路径/前缀


# sa-token配置
sa-token:
  # token名称 (同时也是cookie名称)
  token-name: satoken-server
  # OAuth2.0 配置
  oauth2:
    is-code: true
    is-implicit: true
    is-password: true
    is-client: true



